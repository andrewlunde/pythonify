```
Usage: wex deploy|d [options]

Deploy extension

Options:
  -e, --envHost [value]    Application Studio host
  -c, --wexConfig [value]  WEX environment configuration for deploy (default: "wexConfig.json")
  --verbose                Verbose (default: false)
  -f, --file [value]       Path to extension file (default: "extension.json")
  -n, --name [value]       Extension name
  -h, --help               output usage information

wex deploy -e webide.cry10cf.int.applicationstudio.cloud.sap -n ws-pdnmm -f extension.json --verbose

```
